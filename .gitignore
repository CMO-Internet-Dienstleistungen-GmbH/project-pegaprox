# Python
__pycache__/
*.pyc
*.pyo
venv/
*.egg-info/

# SSL / Secrets
cert.pem
key.pem
ssl/*.key
ssl/*.crt
ssl/*.pem
*.p12
*.pfx
.keys/

# Runtime data
config/
logs/
backups/
*.db
*.db-journal
images/login_bg.png

# Generated at runtime (rewritten from the vms.py heredoc on every start — never edit
# the dot-file directly; it drifts in the working tree on each restart otherwise)
pegaprox/api/.ssh_ws_server.py

# Build artifacts
web/Dev/.build/
web/Dev/config/
web/Dev/logs/
archive/

# OS / IDE
.DS_Store
.vscode/
.idea/
.zed/
*.swp
*.swo

# codeant CLI scratch output — tool state, never source
.codeant/

# Task journal and generated rule mirrors — working memory, never a deliverable.
# The journal names hosts and customers; this repository is public.
.claude/
